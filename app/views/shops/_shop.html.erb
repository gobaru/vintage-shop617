<div class="card">
  <%= link_to image_tag(shop.image, class: :card__img ), shop_path(shop.id)%>
  <div class="card__body">
    <%= link_to shop.shop_name, shop_path(shop.id), class: :card__title%>
    <p class="card__summary">
      <%= shop.bland %>
    </p>
    <%= link_to "by #{shop.user.nickname}★", "/users/#{shop.user_id}", class: :card__user %>
  </div>
</div>